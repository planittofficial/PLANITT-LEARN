# This file is deprecated — use .env.example instead.
#
#   cp .env.example .env.local
#
# Everyone (including interns) uses the same local setup.
# Planitt appbackend URL is not required for local development.
